# Falcon-ipchanger
Advanced Tor IP Rotator written in Bash. Automatically rotates Tor exit nodes, refreshes IP addresses, verifies Tor connectivity, and supports multi-distro Linux systems with a clean terminal interface.
